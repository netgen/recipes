name: Update Flex endpoint

on:
    push:
        branches:
            - master

defaults:
    run:
        shell: bash

jobs:
    call-flex-update:
        uses: netgen/recipes/.github/workflows/callable-flex-update.yml@master
